# Easy Proxies 节点文件示例（建议每行写完整 URI）
# 每行一个节点 URI；名称可用 #后缀（支持 URL 编码）。
# 以 # 开头的是注释，空行会被忽略。

# ====== 上游 HTTP/HTTPS 代理节点（可直接当作节点加入池） ======

# HTTP 代理（明文）
http://203.0.113.10:8080#示例HTTP代理

# HTTPS 代理（HTTP proxy over TLS）
https://203.0.113.11:443?sni=www.bing.com&allowInsecure=1#示例HTTPS代理

# HTTP 代理但需要 TLS（部分列表只给 host:443；可用 query 强制 tls=1）
http://203.0.113.12:443?tls=1&sni=www.bing.com&allowInsecure=1#示例HTTP_TLS代理

# ====== 上游 SOCKS 代理节点 ======

# SOCKS5 代理
socks5://198.51.100.10:1080#示例SOCKS5代理

# SOCKS5 代理（带认证）
socks5://user:pass@198.51.100.11:1080#示例SOCKS5认证

# 带额外描述的列表行（只会读取第一段 URL，其余会忽略）
socks5://198.51.100.12:1080 [[机房] 示例描述会被忽略]

# ====== 常见代理协议节点（示例占位） ======

# VLESS（Reality / TLS / WS / gRPC 等参数可直接从订阅复制）
vless://00000000-0000-0000-0000-000000000000@example.com:443?encryption=none&security=reality&sni=example.com&fp=chrome&pbk=REPLACE_ME&sid=REPLACE_ME&type=tcp&flow=xtls-rprx-vision#示例VLESS

# VMess（Base64 JSON 形式，占位）
vmess://REPLACE_WITH_BASE64_JSON#示例VMess

# Trojan
trojan://password@example.com:443?sni=example.com&allowInsecure=1#示例Trojan

# Shadowsocks（userinfo 为 base64(method:password)，占位）
ss://REPLACE_WITH_BASE64_METHOD_PASSWORD@example.com:8388#示例SS

# Hysteria2
hysteria2://password@example.com:443?sni=example.com&insecure=0#示例Hysteria2

# ====== 提示 ======
# 1) 如果你拿到的是纯 host:port 列表，建议用 subscriptions 并在 URL 后加 #http/#https/#socks5 来指定协议。
# 2) 公共 HTTP/SOCKS 列表通常不稳定，建议配合 pool.failure_threshold: 1 与较短 blacklist_duration（如 10m）。